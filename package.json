{
  "name": "web3-template",
  "version": "1.0.0",
  "description": "Template for Web3 apps with smart contracts, backend and frontend",
  "scripts": {
    "contracts:compile": "cd contracts && npm run compile",
    "contracts:test": "cd contracts && npx hardhat test",
    "contracts:node": "cd contracts && npx hardhat node",
    "contracts:deploy:local": "cd contracts && npx hardhat run scripts/deploy.ts --network localhost",
    "contracts:deploy:sepolia": "cd contracts && npx hardhat run scripts/deploy.ts --network sepolia",
    "frontend:dev": "cd frontend && npm run dev",
    "frontend:build": "cd frontend && npm run build",
    "start": "npm run frontend:dev",
    "dev": "npm run frontend:dev"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0",
    "dotenv": "^16.4.7",
    "hardhat-ethers": "^1.0.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "ethers": "^6.13.5",
    "hardhat": "^2.22.19"
  }
}
